
const Header = () => {
    return (
        <header className="header">
            <h1>
                Wildfire Tracker (Powered by NASA API)
            </h1>
        </header>
    );
};

export default Header;